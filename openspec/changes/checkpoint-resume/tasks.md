## 1. PipelineCheckpoint Model [no dependencies]

- [x] 1.1 Add `PipelineCheckpoint` Pydantic model to `models.py` with fields: `run_id: str`, `change_name: str`, `repo_path: str`, `completed_stage: str` (one of: "worktree", "worker_eval", "pr", "review", "openspec_review"), `worktree_path: str | None`, `branch: str | None`, `branch_head_sha: str | None`, `pr_url: str | None`, `session_id: str | None`, `last_worker_result: WorkerResult | None = None`, `last_eval_result: EvalResult | None = None`, `protected_files: list[str] = []`, `stages: list[StageResultUnion] = []`, `timestamp: str`, `ecosystem: str = "unknown"`, `auto_merge: bool = False`, `skip_review: bool = False`, `review_cycle: list[str] | None = None`.
- [x] 1.2 Add tests: `PipelineCheckpoint` construction. Roundtrip via `model_dump_json()` / `model_validate_json()` — assert `session_id == "sess-123"`, `stages[0].worktree_path == Path("/tmp/wt")` for a WorktreeResult, `stages[1].cost_usd == 0.42` for a WorkerResult. Verify `completed_stage`, `auto_merge`, and `review_cycle` survive roundtrip.

## 2. Checkpoint I/O [depends: 1]

- [x] 2.1 Create `src/action_harness/checkpoint.py` with `write_checkpoint(repo_path: Path, checkpoint: PipelineCheckpoint) -> None`. Writes to `.action-harness/checkpoints/<run_id>.json` using atomic write (temp file + `os.replace()`). Creates the checkpoints directory if needed. Log to stderr on write.
- [x] 2.2 Add `read_checkpoint(repo_path: Path, run_id: str) -> PipelineCheckpoint | None`. Returns None if file doesn't exist. Logs warning on parse error, returns None (never crashes).
- [x] 2.3 Add `find_latest_checkpoint(repo_path: Path, change_name: str) -> PipelineCheckpoint | None`. Reads EACH `.json` file in `.action-harness/checkpoints/`, parses it, and compares `checkpoint.change_name == change_name`. Returns the most recent by `timestamp`. Returns None if no match.
- [x] 2.4 Add `delete_checkpoint(repo_path: Path, run_id: str) -> None`. Deletes the checkpoint file. Logs warning if file doesn't exist (non-fatal).
- [x] 2.5 Add tests: write + read roundtrip, read nonexistent returns None, find_latest with multiple checkpoints returns most recent, find_latest filters by change_name, delete existing file, delete nonexistent logs warning. Verify atomic write pattern.

## 3. Pipeline Checkpoint Integration [depends: 2]

- [x] 3.1 Add a `_write_checkpoint()` helper in `pipeline.py` (or import from checkpoint module). Call it at 5 macro-stage boundaries in `_run_pipeline_inner()`: (1) after worktree creation with `completed_stage="worktree"`, (2) after the worker+eval retry loop completes with eval passing with `completed_stage="worker_eval"`, (3) after PR creation with `completed_stage="pr"`, (4) after the review cycle completes with `completed_stage="review"`, (5) after openspec review with `completed_stage="openspec_review"`. Build the checkpoint from local variables at each point: `worktree_path`, `branch`, `pr_url`, `session_id` (from last WorkerResult), `stages`, `protected_files`, `ecosystem`, `auto_merge`, `skip_review`, `review_cycle`. Capture `branch_head_sha` via `git rev-parse HEAD` in the worktree.
- [x] 3.2 On successful pipeline completion (before returning the final result), call `delete_checkpoint()` to clean up.
- [x] 3.3 Add tests: mock pipeline run produces checkpoint files at each macro-stage. Successful pipeline deletes checkpoint. Failed pipeline preserves checkpoint.

## 4. Resume Logic [depends: 2, 3]

- [x] 4.1 Add `--resume` option to `cli.py` `run` command (`str | None`, default None, accepts "latest" or a run ID string). When provided, call `find_latest_checkpoint` (for "latest") or `read_checkpoint` (for specific ID). Pass the checkpoint to `run_pipeline()`. Update the `run` command docstring to document `--resume`.
- [x] 4.2 In `run_pipeline()`, accept `checkpoint: PipelineCheckpoint | None = None`. When a checkpoint is provided: (a) validate `checkpoint.change_name == change_name` and `checkpoint.repo_path` matches — if mismatch, log warning and start fresh. (b) Validate worktree exists at `checkpoint.worktree_path` — if missing, log warning and start fresh. (c) Validate branch HEAD via `git rev-parse HEAD` in the worktree matches `checkpoint.branch_head_sha` — if mismatch, log warning and start fresh. (d) Pre-populate `stages` from `checkpoint.stages`. (e) Reuse `checkpoint.run_id` as `run_id`. (f) Use `checkpoint.timestamp` as `started_at`. (g) Use checkpoint's `auto_merge`, `skip_review`, `review_cycle` values (override CLI arguments, log warning if they differ). (h) Pass checkpoint to `_run_pipeline_inner`.
- [x] 4.3 In `_run_pipeline_inner()`, accept `checkpoint: PipelineCheckpoint | None = None`. Add a helper `_should_run_stage(stage: str, checkpoint: PipelineCheckpoint | None) -> bool` that returns True if checkpoint is None or if `stage` comes AFTER `checkpoint.completed_stage` in the progression order `["worktree", "worker_eval", "pr", "review", "openspec_review"]`. Wrap each macro-stage block in `if _should_run_stage("stage_name", checkpoint):` guard. In the `else` branch (skipping), restore pipeline locals from checkpoint: `worktree_path = Path(checkpoint.worktree_path)`, `branch = checkpoint.branch`, `worker_result = checkpoint.last_worker_result`, `eval_result = checkpoint.last_eval_result`, and construct a stub `pr_result = PrResult(success=True, stage="pr", pr_url=checkpoint.pr_url, branch=checkpoint.branch)` when skipping the PR stage.
- [x] 4.4 Update dry-run output to show `resume: <run-id>` when `--resume` is provided.
- [x] 4.5 Add tests: (a) resume with `completed_stage="pr"` — verify worktree, worker_eval, and pr stages are skipped, review starts with correct `pr_url` and `worker_result`. (b) Resume with missing worktree starts fresh. (c) Resume with mismatched change name starts fresh. (d) Resume with different branch HEAD starts fresh. (e) `--resume latest` finds most recent checkpoint. (f) `--resume` with no checkpoint starts fresh with warning. (g) Verify `create_pr` receives `worker_result` and `eval_result` from checkpoint when resuming at the pr stage.

## 5. Validation [depends: all]

- [ ] 5.1 Run `uv run pytest -v` — all tests pass
- [ ] 5.2 Run `uv run ruff check .` and `uv run mypy src/` — clean
